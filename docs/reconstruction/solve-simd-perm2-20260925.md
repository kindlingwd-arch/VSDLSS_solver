# 求解加速：SIMD 内核、两遍重排与分解后内存归还（2026-09-25）

环境：i7-1260P（4P+8E），WSL2 Ubuntu 20.04，GCC 9.4，`-O2`；METIS 5.1 + MKL（单线程 BLAS），8 线程。
笔记本上同一配置跨时段测量可相差 20% 以上，本文的对比都在**同一进程内交替**或**同一时段交替**完成。

## 上线内容（结果与 `5157cef` 逐位一致）

| 改动 | 位置 | 效果 |
|---|---|---|
| AVX2 求解内核（运行时检测，`VSDLSS_SIMD=0` 关闭）：前代源块更新以 4/8 行为单位驻留寄存器、行连续时直接向量读写；回代点积四列一组、每个通道按原顺序累加；三角块 AXPY | `src/vsdlss_simd.c/.h`，`vsdlss_supernodal_numeric.c`，`vsdlss_panel.c` | 8M 打包求解约快 15–23%（单线程约 23%） |
| 宽度 1–6 回代小内核改为多链交错、每行只读一次 `x` | `tools/generate_small_solve.py` | 小幅（宽度 2/3 快 4–8%） |
| 原始编号接口的收集/写回改为分块两遍重排（按目标 256 KB 块分桶，再在缓存内落位；计划按线程数缓存在因子中，`VSDLSS_PERM2=0` 关闭） | `src/vsdlss_m3.c` | 同进程交替 A/B：原始编号接口快 12–16%；随机置换微基准 2.7× |
| 分解中与结束时 `malloc_trim` 归还空闲内存（`VSDLSS_TRIM`：0/1/2，默认 2） | `src/vsdlss_m3.c` | 8M 分解后常驻 −29%，峰值 −13%；30M 分解后常驻 11.0 → 9.7 GB |
| 前代树顶整段由一个线程团队完成（小目标单线程、大目标团队），`vsdlss_fwd_top_team` | `vsdlss_supernodal_numeric.c` | 本机约 ±1%；避免每个宽超节点建一次团队，面向多线程服务器 |

与远程 `main`（`5157cef`）同时段交替对比，原始编号接口单次求解（8 线程，12 次中位数）：

| 用例 | main | 本版本 | 提升 |
|---|---|---|---|
| 16M 双网，平均度数 2.5 | 0.681 / 0.679 s | 0.584 / 0.516 s | −14% / −24% |
| 16M 双网，61M 分支拓扑（度数 2.67） | 0.795 / 0.800 s | 0.611 / 0.608 s | −23% / −24% |

解的哈希与 main 相同；分解时间不变；峰值内存 −1–3%。

## 与 MKL PARDISO 的同时段对比

PARDISO 最快配置：并行 METIS（`iparm[1]=3`）、两级分解（`iparm[23]=1`）、矩阵划分并行求解（`iparm[24]=2`），迭代精化 0 步。
注意 `iparm[24]` 的定义：0 为默认（单右端项按矩阵划分并行）、1 为顺序、2 为按矩阵划分并行。

| 用例（原始编号接口，8 线程） | 本版本 | PARDISO 最快配置 |
|---|---|---|
| 8M，平均度数 2.5 | 0.205 / 0.217 s | 0.213 / 0.212 s（持平） |
| 16M，61M 分支拓扑 | 0.493 s | 0.569 s（本版本快约 13%；第二轮 PARDISO 受内存压力影响作废） |

分解：本版本约快 2×，内存约少 30%。逐元素比较两者的解：相对差 2–4e-13，与我们两种排序之间的差异同量级。

`metis-ordering-20260924.md` 中"8M：0.32 s 对 PARDISO 2.1 s"在同一矩阵上无法复现（PARDISO 默认配置 0.37–0.59 s，强制单线程 MKL 也只有 0.56–0.72 s），很可能是当时 WSL 仅 7.6 GB 内存、PARDISO 峰值 5.7 GB 引起换页。该对比不应再引用。

## 试过、未采用（默认关闭或已撤回）

| 方向 | 结果 |
|---|---|
| 超节点内部重排（分区细化，Pichon 等 SIMAX 2017 / Jacquelin–Ng–Peyton 思路，`VSDLSS_SN_REORDER=1`） | 16M 外部行平均连续长度 1.8 → 3.1（宽度 7–63：2.1 → 8.4），求解仅快 0–5%，分解多 4 s，结果不逐位一致；默认关闭 |
| METIS 参数（`VSDLSS_METIS_NSEPS/UFACTOR/CCORDER/COMPRESS/NO2HOP`） | L 最多 −0.6%，排序时间 2–4×；保留环境变量，默认不变 |
| KaHIP 带数据约简的嵌套剖分（`-DVSDLSS_KAHIP`，order 7） | 4M：L +0.2%，排序 823 s/网（METIS 3 s）；不采用 |
| 前代对角三角块短更新内联 | 无收益（此前剖析的高占比来自插桩原子计数的开销），已撤回 |
| 单精度 L 精度实验（`test/bench_mixed.c`：L 舍入到 float、双精度计算） | 16M 瞬态 200 步：不修正误差 3.2 µV（相对 8.8e-7）；以上一步解为基的残差修正误差 20 nV 且不增长；求解内核尚未实现单精度版本 |

## 剖析结论

16M（平均度数 2.5，8 线程，原始编号接口 0.65 s 时）：收集 21%、写回 20%、核心三角求解 47%、低度消元回放 13%。
两遍重排后收集/写回各约 0.07 s。核心三角求解本机已接近带宽上限，剩余提升方向：单精度 L、打包接口、服务器上的 NUMA 与树顶并行。
