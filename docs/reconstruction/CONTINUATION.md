# 开发续接记录

最后更新：2026-09-08。

## 用户目标

依据反编译代码重建线性方程求解器；先整理具体原理和参考文档，再开发；工作必须能跨会话续接。

## 当前状态

- 已定位实际项目 `D:\vsdlss\vsdlss_solver`。
- 已核对 688 个反编译 C 文件，生成来源清单及 SHA-256。
- 已静态审计主驱动、内存/磁盘分块流程、符号消元、数值调度、求解、块分配，以及现有重建核心和部分兼容代码。
- 已写原理证据、现有问题表、重建设计草案与验收标准。
- 未逐个恢复 688 个函数，未宣称原版 ABI、一般 LU 或磁盘格式已经恢复。
- 本轮未修改现有求解器实现。M1 尚未开始，设计草案待用户评审。

## 环境与验证结果

- Windows PowerShell，项目没有 Git 元数据。
- PATH 未找到 gcc/clang/make/cmake；Python 位于 `C:\Python314\python.exe`。
- `wsl --list --quiet` 返回 `Wsl/EnumerateDistros/Service/E_ACCESSDENIED`。
- 未执行 C 构建或数值回归。旧 README 的运行数字不是本轮证据。

## 下一次从这里继续

1. 先读本目录 README、审计表和设计草案，不重新全文读取全部反编译文件。
2. 按用户对方案的回复确认 M1 范围；默认建议保留 C 核心、先做 SPD 内存闭环，再扩展排序/分块。
3. 确认可用 C 工具链：可检查常见安装目录或恢复对已有 WSL 的访问；不要把环境访问失败写成代码错误。
4. 依据设计写逐任务实现计划，具体到接口签名、文件和测试命令。尚未存在可直接执行的详细计划。
5. 先建立回归测试，重点覆盖审计 A01/A04/A05/A06/A11；修复 RCM 双射和方向、输入规范化、非有限数拒绝及测试退出码。
6. 然后统一因子/API 所有权与状态，处理 I/O 和重复 RHS。旧块/MLD 占位路径不列入 M1 完成能力。
7. 每完成一个验收单元，更新这里的实际命令、退出码、误差和下一项。

## 待补证据

- 原始可执行程序、数据段常量及依赖在本机是否可取得；当前未确认。
- `factorWithinBlock_r` / `denseCholesky` 等无 `_vsdlss` 后缀依赖的源码闭包。
- 原始单块因子采用何种对角归一化，LLᵀ/LDLᵀ 及部分分解变换的精确关系。
- 原版一般非对称求解、主元及失败语义。
- 原版真实矩阵/RHS 样本、端序、重复项及 `.CLG` 完整协议。
- 现有 CSparse 派生实现的确切来源版本及许可证材料。

## 可直接用于下一次会话的指令

> 请继续 `D:\vsdlss\vsdlss_solver` 的 VSDLSS 重建。先读 `docs/reconstruction/CONTINUATION.md` 和同目录设计、审计文档，按当前已批准范围继续。不要把旧 README 的声明当作验证结果；先补可复现测试，再实现，并把新证据和下一步写回续接记录。
